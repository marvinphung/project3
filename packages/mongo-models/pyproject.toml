[project]
name = "footballpulse-mongo-models"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "beanie>=2.0,<3",
    "footballpulse-shared",
]

[tool.uv.sources]
footballpulse-shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
