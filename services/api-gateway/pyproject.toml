[project]
name = "footballpulse-api-gateway"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[project.scripts]
footballpulse-api-gateway-health = "footballpulse_api_gateway.health:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
