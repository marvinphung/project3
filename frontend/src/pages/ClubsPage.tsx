import { useState } from 'react'
import { Link } from 'react-router'
import { usePublicEntities } from '../api/hooks'
export default function ClubsPage() { const [query, setQuery] = useState(''); const remote = usePublicEntities('CLUB', query); return <div className="max-w-[1280px] mx-auto px-4 sm:px-6 py-8"><h1 className="text-3xl font-bold mb-4">Câu lạc bộ</h1><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Tìm câu lạc bộ..." className="w-full sm:w-64 px-3 py-2 border rounded-lg mb-6" />{remote.loading ? <p>Đang tải…</p> : remote.error ? <p className="text-red-600">{remote.error.message}</p> : remote.data?.length ? <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{remote.data.map((item) => <Link key={item.id} to={`/clb/${item.slug}`} className="bg-white border rounded-xl p-4"><h2 className="font-semibold">{item.name}</h2><p className="text-sm text-[#6B7280]">{item.article_count} bài viết · {item.story_count} Story</p></Link>)}</div> : <p className="text-[#6B7280] py-12">Chưa có CLB có dữ liệu.</p>}</div> }
